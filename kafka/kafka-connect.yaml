apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnect
metadata:
  name: my-connect-cluster
  annotations:
    strimzi.io/use-connector-resources: "true"
spec:
  version: 3.7.1
  replicas: 3
  bootstrapServers: my-cluster-kafka-bootstrap:9093
  config:
    group.id: connect-cluster
    offset.storage.topic: connect-cluster-offsets
    config.storage.topic: connect-cluster-configs
    status.storage.topic: connect-cluster-status
    config.storage.replication.factor: -1
    offset.storage.replication.factor: -1
    status.storage.replication.factor: -1
  build:
    output:
      type: docker
      image: docker.io/mmm09/my-connect-cluster:latest
      pushSecret: dockerhub-secret
    plugins:
      - name: debezium-mysql
        artifacts:
          - type: jar
            url: https://repo1.maven.org/maven2/io/debezium/debezium-connector-mysql/1.9.7.Final/debezium-connector-mysql-1.9.7.Final-plugin.tar.gz
            sha512sum: e8f0cdfb2f2c15d8c4685bc4736fd1a94f80bda0327dd02e771a10c8d285b4b8c1e17e7c583b55a174cb375ccd047156e67de44cdb50c7634a5710d7f60f6049
      - name: debezium-postgres-connector
        artifacts:
          - type: zip
            url: https://repo1.maven.org/maven2/io/debezium/debezium-connector-postgres/2.1.1.Final/debezium-connector-postgres-2.1.1.Final-plugin.zip
            sha512sum: a647a5c4ada510ba521ecdbdc789c01ee73675bb9a97dbf45d0f70251be778360aa52de62b60f8f2791f875d95e7793cad96c5dcddb911c2cea79ea40e85fbe4

# apiVersion: kafka.strimzi.io/v1beta2
# kind: KafkaConnect
# metadata:
#   name: my-connect-cluster
# #  annotations:
# #  # use-connector-resources configures this KafkaConnect
# #  # to use KafkaConnector resources to avoid
# #  # needing to call the Connect REST API directly
# #    strimzi.io/use-connector-resources: "true"

# #.
# # building custom kafka connect https://anhcodes.dev/blog/deploy-debezium-aks/#6-deploy-kafka-connect-cluster
# spec:
#   version: 3.7.1
#   replicas: 3
#   bootstrapServers: my-cluster-kafka-bootstrap:9093
#   #tls:
#     # trustedCertificates:
#     #   - secretName: my-cluster-cluster-ca-cert
#     #     certificate: ca.crt
#   config:
#     group.id: connect-cluster
#     offset.storage.topic: connect-cluster-offsets
#     config.storage.topic: connect-cluster-configs
#     status.storage.topic: connect-cluster-status
#     # -1 means it will use the default replication factor configured in the broker
#     config.storage.replication.factor: -1
#     offset.storage.replication.factor: -1
#     status.storage.replication.factor: -1
