kubectl create namespace kafka
kubectl create -f 'https://strimzi.io/install/latest?namespace=kafka' -n kafka
kubectl get pod -n kafka --watch
kubectl apply -f kafka.yaml -n kafka 
kubectl logs deployment/strimzi-cluster-operator -n kafka -f


docker login
cat ~/.docker/config.json
kubectl create secret generic dockerhub-secret \
    --from-file=.dockerconfigjson=$HOME/.docker/config.json \
    --type=kubernetes.io/dockerconfigjson -n kafka 


kubectl get secret dockerhub-secret -n kafka  --output=yaml
kubectl apply -f kafka-connect.yaml -n kafka 


kubectl exec -it mysql-replica-8d8858b47-dvzbw -n kafka -- /bin/bash 
mysql -u mysqluser -p
mysqlpw
update inventorycustomers set first_name="Sally Marie" where id=1001;
